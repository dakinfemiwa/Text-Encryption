import tkinter
from tkinter import ttk
from tkinter import *

root = Tk()
root.geometry('1000x500')

#Constants
rowLength = 10
columnLength = 10

for x in ro
label = ttk.Label(text="Game")
label.grid(row=0, column=0)

label = ttk.Label(root, text=0)

label for row in range(rowLength) for column in range(columnlength)

